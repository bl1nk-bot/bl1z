# ─── Clippy configuration for BAMS workspace ─────────────
# Run: cargo clippy --all-targets --all-features

# Thresholds
cognitive-complexity-threshold = 30
type-complexity-threshold = 500
too-many-arguments-threshold = 10

# Lints to allow/deny (applied via -D warnings in Makefile)
# See: https://doc.rust-lang.org/clippy/lint_settings.html